import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent {

  /* LO MOVEMOS PARA GENERAR UN NUEVO COMPONENTE Y YA NO PERTENECE AQUI
  
  titulo: string = 'Contador App';
  numero: number = 10;
  base: number = 5;

  // base: numero = 5;
  acumular( valor: number ) {
    this.numero += valor;
  }

  */
  /* Clase 39
  acumular( valor: number ) {
    this.numero += valor;
  }
  */
}

